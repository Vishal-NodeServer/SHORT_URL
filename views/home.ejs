<!DOCTYPE html>
<html lang="en">
    <head>
        <style>
       body{
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
       }

    </style>
    </head> 
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>

    <h1>URL Shortener</h1>
    <% if (locals.id) { %>
      <p>Url Generateted : http://localhost:8003/url/<%= id %></p>
   <% } %>
    <div>
        <form method="post" action="/url">
            <label>Enter Your Original URL</label>
            <input name="url" type="text" placeholder="https://example.com"/>
            <button type="submit">Generate</button>
        
        </form>



    </div>
    <div>
      <% if(locals.urls) { %>
     <table>
         <thead>
             <th>S. No</th>
             <th>ShortID</th>
             <th>Visted</th>
         </thead>
         <tbody>
            <% urls.forEach((url , index )=> { %>
<tr>
                <td><%= index + 1 %></td>
                <td><%= url.shortId %></td>
                <td><%= url.vistHistory.length %></td>
            </tr>
           <% })  %>
         </tbody>

     </table>

     <% } %>



    </div>


</body>
</html>